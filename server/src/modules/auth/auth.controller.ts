import { Request, Response } from "express";

export const authRegister = (req: Request, res: Response) => {
  res.send("ruta para registrar");
  // loguin
};

export const authLogin = (req: Request, res: Response) => {
  res.send("ruta para loguear");
  // loguin
};


